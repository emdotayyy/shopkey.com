import classes from "./ProductsSummary.module.css";

const ProductsSummary = () => {
    return (
        <section className={classes.summary}>
            <h2>Engages and retains buyers at every level!</h2>
            <p>Allows customers to browse and purchase items from an online store.</p>
            <p>This aims at providing an immersive and unique experience that makes online shopping a much simpler and seamless process.</p>
        </section>
    );
};

export default ProductsSummary;